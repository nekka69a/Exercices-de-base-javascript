// Ajoute 2 nombres 40 et 50 à la fin du tableau, puis retire un et affiche le

const nombres = [10, 20];

nombres.push(40, 50);

console.log(nombres);

const nombreRetire = nombres.pop();

console.log(nombres);

console.log(nombreRetire);
